[package]
name = "protobuf-src"
version = "1.1.0"
edition = "2021"
build = "build.rs"
description = "Local no-op stub. Real protoc is provided via PROTOC env var."
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"
